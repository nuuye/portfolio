import React from 'react';
import './Footer.scss';

const Footer: React.FC = () => {
    return (
        <footer className="footer">
            <span>© Built by Thomas Mostowfi using React.js, SCSS and Material UI.</span>
        </footer>
    );
};

export default Footer;
